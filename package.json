{
  "name": "homeschool",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\" --names \"client,server\" --prefix-colors \"cyan,magenta\"",
    "setup": "concurrently \"cd client && npm install\" \"cd server && npm install\" --names \"client,server\" --prefix-colors \"cyan,magenta\""
  }
}